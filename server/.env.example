# ── Local fallback (used only when DATABASE_URL / DIRECT_URL are not set) ──
PGHOST=localhost
PGPORT=5432
PGDATABASE=aadhyapath
PGUSER=postgres
PGPASSWORD=your-local-postgres-password

# ── Supabase managed Postgres (recommended in production) ──
# IMPORTANT: if your password contains special characters like '@', URL‑encode them (e.g. '@' → '%40').
# Primary connection via PgBouncer pooling
DATABASE_URL=postgresql://postgres.itniteawqzjuympwxorv:Your%40Password@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true
# Direct connection for migrations / one-off scripts
DIRECT_URL=postgresql://postgres.itniteawqzjuympwxorv:Your%40Password@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres

PORT=5180

# Supabase JWT secret is required to validate dashboard requests (Settings > API > JWT secret)
SUPABASE_JWT_SECRET=
# Public Supabase project settings for the browser client
SUPABASE_URL=https://itniteawqzjuympwxorv.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Iml0bml0ZWF3cXpqdXltcHd4b3J2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTg3NzIwMzAsImV4cCI6MjA3NDM0ODAzMH0.k9HsSPabFeecC3LNpti4gBcMCC7FWasj2UcKQkBORxk

# Optional email notifications (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="AadhyaPath <no-reply@aadhyapath.example.com>"

# For hosted deployments (optional)
# DATABASE_URL=postgres://user:password@host:5432/aadhyapath
# Allow calls from GitHub Pages origin(s), comma-separated (include both apex and repo pages URLs if needed)
CORS_ORIGINS=https://israfil-03.github.io,https://israfil-03.github.io/Disaster-Management
