PGHOST=localhost
PGPORT=5432
PGDATABASE=aadhya_path
PGUSER=postgres
PGPASSWORD=Israfil@951
PORT=5180

# For hosted deployments (optional)
# DATABASE_URL=postgres://user:password@host:5432/aadhya_path
# Allow calls from GitHub Pages origin(s), comma-separated (include both apex and repo pages URLs if needed)
# CORS_ORIGINS=https://<username>.github.io,https://<username>.github.io/<repo>
