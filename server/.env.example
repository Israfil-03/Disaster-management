# Database Configuration
PGHOST=localhost
PGPORT=5432
PGDATABASE=aadhyapath
PGUSER=postgres
PGPASSWORD=your_password_here
PORT=5174

# For hosted deployments (optional)
# DATABASE_URL=postgres://user:password@host:5432/aadhyapath

# CORS Configuration (comma-separated origins for production)
# Allow calls from GitHub Pages origin(s), comma-separated
# Include both apex and repo pages URLs if needed
CORS_ORIGINS=https://your-username.github.io,https://your-username.github.io/your-repo-name
